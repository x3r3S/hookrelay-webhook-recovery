{
  "name": "hookrelay",
  "version": "1.0.0",
  "private": true,
  "description": "Local webhook inspection, idempotency and recovery with reproducible audit evidence.",
  "license": "SEE LICENSE IN PORTFOLIO-REVIEW-LICENSE.md",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node scripts/serve.mjs",
    "test": "npm run test:unit && npm run test:e2e",
    "test:unit": "node --test tests/*.test.mjs",
    "test:e2e": "playwright test",
    "check": "node --check src/domain.mjs && node --check src/proof.mjs && node --check src/app.mjs && node --check scripts/print-proof.mjs && node --check scripts/serve.mjs && node --check playwright.config.mjs && node --check tests/e2e/hookrelay.spec.mjs",
    "proof:print": "node scripts/print-proof.mjs"
  },
  "devDependencies": {
    "@playwright/test": "^1.62.1"
  }
}
