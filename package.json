{
  "name": "hookrelay",
  "version": "1.0.0",
  "private": true,
  "description": "Local webhook inspection, idempotency and recovery with reproducible audit evidence.",
  "license": "SEE LICENSE IN PORTFOLIO-REVIEW-LICENSE.md",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node scripts/serve.mjs",
    "test": "node --test tests/*.test.mjs",
    "check": "node --check src/domain.mjs && node --check src/proof.mjs && node --check src/app.mjs && node --check scripts/print-proof.mjs && node --check scripts/serve.mjs",
    "proof:print": "node scripts/print-proof.mjs"
  }
}
